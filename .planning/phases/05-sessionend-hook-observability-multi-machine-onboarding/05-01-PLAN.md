---
phase: 05-sessionend-hook-observability-multi-machine-onboarding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sync_chats.py
  - tests/test_phase5_last_run.py
  - tests/test_phase5_write_session.py
autonomous: true
requirements:
  - OBSERV-03
must_haves:
  truths:
    - "`_write_session(session_id, label_dict, config, state) -> str` exists and both cmd_write and (future) cmd_once use it as the sole write path"
    - "`_write_last_run(dict)` writes ~/.claude-chat/last_run.json atomically with tmp+fsync+rename and preserves the previous version at last_run.json.bak"
    - "`LAST_RUN_PATH` resolves from `CLAUDE_CHAT_HOME` (honors the env override used by tests)"
    - "Existing 159-test suite stays green after the extraction (no behavior change in cmd_write)"
  artifacts:
    - path: sync_chats.py
      provides: "_write_session helper + _write_last_run helper + LAST_RUN_PATH constant"
      contains: "def _write_session("
    - path: tests/test_phase5_last_run.py
      provides: "Unit tests for _write_last_run atomic writer + schema round-trip"
    - path: tests/test_phase5_write_session.py
      provides: "Unit tests for _write_session contract (stub dict in, status string out)"
  key_links:
    - from: sync_chats.py::cmd_write
      to: sync_chats.py::_write_session
      via: "direct function call after stdin JSON parse"
      pattern: "_write_session\\("
    - from: sync_chats.py::_write_last_run
      to: LAST_RUN_PATH
      via: "CLAUDE_CHAT_HOME / last_run.json"
      pattern: "LAST_RUN_PATH"
---

<objective>
Extract the core write logic from `cmd_write` into a reusable `_write_session(session_id, label_dict, config, state) -> str` helper so Plan 02's `cmd_once` can call it in-process with a stub dict (D-02 stdin contract preservation). Add the `_write_last_run(dict)` atomic writer and `LAST_RUN_PATH` module-level constant (D-11, D-14). No user-visible behavior changes — cmd_write continues to read stdin and delegate to _write_session; the full existing test suite stays green.

Purpose: Research flagged `_write_session` extraction as the critical refactor enabling Plans 02 and 03. Done as a standalone wave-1 foundation so downstream plans consume a stable internal contract. `_write_last_run` is colocated here because it reuses `_write_atomic`'s template (D-14: "same pattern as state.json") and is referenced by every other plan.

Output: `sync_chats.py` with `_write_session`, `_write_last_run`, `LAST_RUN_PATH`; two new test files proving the contracts hold.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-sessionend-hook-observability-multi-machine-onboarding/05-CONTEXT.md
@.planning/phases/05-sessionend-hook-observability-multi-machine-onboarding/05-RESEARCH.md
@.planning/phases/05-sessionend-hook-observability-multi-machine-onboarding/05-VALIDATION.md
@sync_chats.py
@tests/test_sync_chats.py

<interfaces>
From sync_chats.py (existing — do not change signatures):

```python
CLAUDE_CHAT_HOME: Path  # Path(os.environ.get("CLAUDE_CHAT_HOME", ...))
CONFIG_PATH = CLAUDE_CHAT_HOME / "config.json"
STATE_PATH  = CLAUDE_CHAT_HOME / "state.json"
LOG_PATH    = CLAUDE_CHAT_HOME / "sync.log"

def _write_atomic(path: Path, data: dict) -> None  # line 64 — tmp+fsync+rename+.bak template
def _load_json(path: Path) -> dict                 # line 98 — returns {} on missing/malformed
def load_state() -> dict                           # line 113
def save_state(state: dict) -> None                # line 132
def _require_config() -> dict                      # line 156 — exits 2 on pre-flight error
def make_stub_label(jsonl_path: Path, session_id: str) -> dict   # line 334
def discover_sessions(state: dict) -> list         # line 174
def cmd_write(args) -> None                        # line 1028 — currently monolithic; extract its core

# Label dict schema (Phase 1 D-02 — contract preserved):
# {title: str, gist: str|null, tags: list[str], coherence_score: int|null, needs_review: bool}
# Unknown keys are ignored (forward-compat). Missing title is a fatal error for that session.
```

New module-level constant to add (D-14):

```python
LAST_RUN_PATH = CLAUDE_CHAT_HOME / "last_run.json"
```

New helper signatures (this plan creates these):

```python
def _write_session(session_id: str, label: dict, config: dict, state: dict) -> str:
    """Return one of: 'synced' | 'skipped' | 'failed' | 'reconciled' | 'edited'.
    Raises on unexpected internal errors (caller catches + records)."""

def _write_last_run(data: dict) -> None:
    """Atomic-overwrite ~/.claude-chat/last_run.json with .bak preservation (D-14).
    Near-copy of _write_atomic. Path = LAST_RUN_PATH."""
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for _write_last_run (OBSERV-03 atomic writer)</name>
  <files>tests/test_phase5_last_run.py</files>
  <behavior>
    - `TestWriteLastRun::test_creates_file_with_expected_schema`: Given the D-11 schema dict, `_write_last_run(d)` creates `CLAUDE_CHAT_HOME/last_run.json` with identical JSON contents (json.loads round-trip == d).
    - `TestWriteLastRun::test_preserves_previous_as_bak`: First call writes v1; second call with different dict writes v2 AND leaves v1 at `last_run.json.bak`.
    - `TestWriteLastRun::test_atomic_no_tmp_leftover`: After a successful write, `last_run.json.tmp` does NOT exist.
    - `TestWriteLastRun::test_honors_claude_chat_home_env`: With `CLAUDE_CHAT_HOME` overridden to a temp dir, the file lands in the temp dir — never in `~/.claude-chat/`.
    - `TestWriteLastRun::test_schema_fields_present`: Round-tripped dict has keys `schema_version, run_started_at, run_finished_at, trigger, machine_label, hostname, synced, skipped, failed, flagged_for_review, mempalace_mined, mempalace_reason, exit_code, errors`.
    Pattern: copy setUp from `tests/test_mine.py` (CLAUDE_CHAT_HOME override via env + tempfile.mkdtemp; reload sync_chats module so module-level paths rebind). Use `importlib.reload(sync_chats)` after setting env so `LAST_RUN_PATH` picks up the temp dir (per RESEARCH.md testing pattern, line ~620).
  </behavior>
  <action>
    Create `tests/test_phase5_last_run.py` with the five tests above. Tests must import `sync_chats` and reference `sync_chats._write_last_run` + `sync_chats.LAST_RUN_PATH`. Per memory `reference_pytest_pipx.md`, use plain `unittest.TestCase` (no pytest-specific features) so both `pipx run pytest` and `python -m unittest` discover them. Tests MUST fail now (helper does not exist yet). Commit: `test(5-01): add failing tests for _write_last_run — OBSERV-03 (5-01-01 RED)`.
  </action>
  <verify>
    <automated>pipx run pytest tests/test_phase5_last_run.py -x 2>&1 | grep -E "(AttributeError|ImportError|FAILED)" &amp;&amp; echo RED_OK</automated>
  </verify>
  <done>File `tests/test_phase5_last_run.py` exists with 5 tests that fail with AttributeError on `sync_chats._write_last_run` or `sync_chats.LAST_RUN_PATH`. Commit landed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement LAST_RUN_PATH + _write_last_run (GREEN)</name>
  <files>sync_chats.py</files>
  <behavior>
    - `LAST_RUN_PATH` module-level constant equals `CLAUDE_CHAT_HOME / "last_run.json"` (colocated with `STATE_PATH`, `LOG_PATH` around lines 34-36).
    - `_write_last_run(data: dict) -> None` mirrors `_write_atomic()` (line 64): writes to `LAST_RUN_PATH.with_suffix('.tmp')`, fsyncs, `shutil.copy2` current file to `.bak` if it exists, `os.replace(tmp, LAST_RUN_PATH)`.
    - JSON serialization: `json.dumps(data, indent=2)` (readability > compactness; no `sort_keys` per D-14/discretion — preserve caller's key order).
    - Inline comments explain: why tmp+fsync+rename is atomic on APFS, why `.bak` is `shutil.copy2` (preserves mtime for post-mortem), why no `sort_keys` (jq-friendly order).
  </behavior>
  <action>
    In `sync_chats.py`:
    1. Add `LAST_RUN_PATH = CLAUDE_CHAT_HOME / "last_run.json"` immediately after `LOG_PATH` (around line 36). Inline comment: `# New in Phase 5 (D-11/D-14): machine-readable record of the most-recent run; atomic-overwrite each run with .bak preservation.`
    2. Add `_write_last_run(data: dict) -> None` directly below `_write_atomic` (around line 95) — near-copy of `_write_atomic` with a section comment pointing out what's shared and why (per D-14 + CONTEXT.md "Python beginner" note). Do NOT parameterize `_write_atomic` itself — D-14 explicitly asks for a near-copy for readability.
    3. Run tests — they should now pass. Commit: `feat(5-01): add _write_last_run + LAST_RUN_PATH — OBSERV-03 (5-01-02 GREEN)`.
  </action>
  <verify>
    <automated>pipx run pytest tests/test_phase5_last_run.py -x -v</automated>
  </verify>
  <done>All 5 tests in `tests/test_phase5_last_run.py` pass. `LAST_RUN_PATH` and `_write_last_run` exist in sync_chats.py. Commit landed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write failing tests for _write_session contract</name>
  <files>tests/test_phase5_write_session.py</files>
  <behavior>
    - `TestWriteSession::test_returns_synced_on_fresh_write`: Given a valid stub label dict and a session not in state, returns literal string `"synced"` and a new file exists in vault.
    - `TestWriteSession::test_returns_skipped_on_already_synced`: When session_id is already in `state['synced_session_ids']`, returns `"skipped"` and writes no file.
    - `TestWriteSession::test_stub_sentinel_hash_supported`: When `label["auto_label_hash_override"] = "stub"` is passed, the written file's frontmatter contains `auto_label_hash: stub` (D-03 — stub sentinel path; Plan 02 will pass this key). [This test MAY fail at implementation time if we choose a different key name in Task 4; that's fine — adjust test to match chosen contract in Task 4.]
    - `TestWriteSession::test_raises_on_missing_title`: A label dict without `title` raises ValueError (or similar — match current cmd_write fatal error behavior).
    - `TestWriteSession::test_updates_state_on_success`: After a `synced` return, `state['synced_session_ids']` contains the session_id and `state.json` on disk reflects the update.
    Uses the same test harness as existing `tests/test_sync_chats.py` clobber tests (CLAUDE_CHAT_HOME + CLAUDE_PROJECTS_DIR env overrides; synthetic JSONL fixtures).
  </behavior>
  <action>
    Create `tests/test_phase5_write_session.py`. Follow the setUp pattern from existing `tests/test_mine.py` (env overrides + module reload). Build a minimal JSONL fixture inline (first user message + one assistant response) via `jsonl_path.write_text(...)`. Tests reference `sync_chats._write_session`. Must fail now — helper does not exist. Commit: `test(5-01): add failing tests for _write_session contract (5-01-03 RED)`.
  </action>
  <verify>
    <automated>pipx run pytest tests/test_phase5_write_session.py -x 2>&1 | grep -E "(AttributeError|FAILED)" &amp;&amp; echo RED_OK</automated>
  </verify>
  <done>File `tests/test_phase5_write_session.py` exists with 5 tests that fail with AttributeError on `sync_chats._write_session`. Commit landed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Extract _write_session from cmd_write (GREEN + regression-safe)</name>
  <files>sync_chats.py</files>
  <behavior>
    - `def _write_session(session_id: str, label: dict, config: dict, state: dict) -> str` contains steps 3-9 of the existing `cmd_write` docstring (lines 1028-1041): clobber layer 1 check, JSONL location, body rendering via `_get_markdown_body`, frontmatter build, O_CREAT|O_EXCL write, reconcile path, state update.
    - Return values: `"skipped"` if session already in `synced_session_ids`; `"synced"` on successful new write; `"reconciled"` or `"edited"` from the existing `_reconcile_crash` classifier (currently returns those strings); the current code path that prints/logs on success is retained by calling the same `_log_sync` lines.
    - `cmd_write` becomes a thin wrapper: (a) `_require_config()`, (b) `load_state()`, (c) `sys.stdin.read() → json.loads → label`, (d) validate title, (e) `result = _write_session(args.session_id, label, config, state)`, (f) `print(result)` mirroring current stdout messages.
    - Stub sentinel support: `_write_session` checks `label.get("auto_label_hash_override")`; if equal to the literal `"stub"`, inject `auto_label_hash: "stub"` into frontmatter INSTEAD OF the SHA-256 computed from body bytes. This is the D-03 path Plan 02 uses. (Any other value is ignored — defensive.) Inline comment: `# D-03: cmd_once passes "stub" so interactive SKILL can detect + re-label later.`
    - `auto_label_hash` computation: if no override, compute `sha256(body_bytes).hexdigest()` as before (Phase 1 D-24 unchanged).
    - All 159 existing tests continue to pass.
  </behavior>
  <action>
    In `sync_chats.py`:
    1. Read `cmd_write` (lines 1028-1229). Identify the steps that become `_write_session` (everything after `label` is parsed from stdin, except the final print/exit).
    2. Create `def _write_session(session_id, label, config, state) -> str` above `cmd_write` (around line 1025). Move the logic verbatim, changing `args.session_id` → `session_id` and returning strings instead of printing terminal messages. For backwards compat, keep the `print("skipped: already_synced")` etc. lines in `cmd_write` (driven by the returned status).
    3. Add the `auto_label_hash_override == "stub"` branch in the frontmatter-build section (D-03).
    4. Rewrite `cmd_write` to: parse stdin → validate title → call `_write_session` → print status → exit per policy (Phase 1 D-31: 0 on synced/skipped, 1 on per-session failure for raised exceptions).
    5. Run full test suite to confirm zero regressions.
    6. Commit: `refactor(5-01): extract _write_session helper + stub-sentinel override path (5-01-04 GREEN)`.
    WHY not parameterize `_write_atomic` for _write_last_run (Task 2): per D-14 + CONTEXT.md "Python beginner" note, near-copy with a section comment is more readable than a generic helper. WHY extract `_write_session` but not inline _write_last_run: `_write_session` is a 100-line block shared across two callers (cmd_write + Plan 02's cmd_once); `_write_last_run` is a 10-line atomic writer.
  </action>
  <verify>
    <automated>pipx run pytest tests/ -x -q</automated>
  </verify>
  <done>`_write_session` exists, all 5 new tests pass, all 159 pre-existing tests still pass. cmd_write is a thin wrapper delegating to _write_session. Stub sentinel override path works. Commit landed.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary    | Description                                                                                                                                                                                                                              |
| ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| test→module | Tests set `CLAUDE_CHAT_HOME` env var to control where files land; importlib.reload is used to rebind module-level paths. Untrusted in principle, but tests ARE the attacker model we care about here (ensuring env override is honored). |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                               | Disposition | Mitigation Plan                                                                                                                                                                                           |
| ---------- | ---------------------- | ------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-05-01-01 | Tampering              | `_write_last_run` hardcoded path                        | mitigate    | Derive from `LAST_RUN_PATH = CLAUDE_CHAT_HOME / "last_run.json"` so env override prevents tests from polluting `~/.claude-chat/` (RESEARCH.md Pitfall 3). Verified by `test_honors_claude_chat_home_env`. |
| T-05-01-02 | Tampering              | `_write_session` silent behavior change during refactor | mitigate    | Full existing test suite (159 tests) must pass after extraction — regression net is the mitigation. `verify` for Task 4 runs `pipx run pytest tests/` (not just phase5 tests).                            |
| T-05-01-03 | Information disclosure | `errors[]` in last_run.json                             | accept      | Phase 5 is single-user local; `str(e)` may include local paths, but the file is mode 0644 in `~/.claude-chat/` (not synced to iCloud per CORE-04). No attack surface.                                     |
| T-05-01-04 | Denial of service      | unbounded `.bak` chain                                  | accept      | Only one `.bak` is kept (overwritten each run); no chain growth. Matches `_write_atomic` precedent.                                                                                                       |

</threat_model>

<verification>
- `pipx run pytest tests/test_phase5_last_run.py tests/test_phase5_write_session.py -v` — all 10 new tests pass
- `pipx run pytest tests/ -q` — full suite green (159 existing + 10 new = 169 tests)
- `grep -n "LAST_RUN_PATH" sync_chats.py` — constant defined exactly once at module level
- `grep -n "def _write_session" sync_chats.py` — helper defined exactly once
</verification>

<success_criteria>

- `_write_session(session_id, label, config, state) -> str` exists, returns the documented status strings, and is called by both `cmd_write` (Task 4) and (in Plan 02) `cmd_once`.
- `_write_last_run(dict)` writes `LAST_RUN_PATH` atomically with `.bak` preservation and honors `CLAUDE_CHAT_HOME` env override.
- Stub sentinel override (`label["auto_label_hash_override"] = "stub"`) produces `auto_label_hash: stub` in frontmatter without affecting the default SHA-256 path.
- Zero regressions: full test suite green.
- Four atomic commits (test-RED → impl-GREEN × 2 pairs).
  </success_criteria>

<output>
After completion, create `.planning/phases/05-sessionend-hook-observability-multi-machine-onboarding/05-01-SUMMARY.md` per the standard summary template.
</output>
