---
phase: 02-skill-md-ai-labeling
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - ~/.claude/skills/sync-chats/SKILL.md
  - tests/test_phase2_labels.py
autonomous: false
requirements:
  - LABEL-02
  - LABEL-07
must_haves:
  truths:
    - "Sessions with fewer than 2 user messages are skipped entirely (no file written)"
    - "The skill processes sessions sequentially via scan then label then write for each"
    - "The complete scan-label-write loop works end-to-end when invoked as /sync-chats"
  artifacts:
    - path: "~/.claude/skills/sync-chats/SKILL.md"
      provides: "Complete, tested skill with ultra-short skip logic and full orchestration"
      contains: "sync_chats.py scan"
    - path: "tests/test_phase2_labels.py"
      provides: "Edge case tests for ultra-short skip and user message counting"
      contains: "test_ultra_short"
  key_links:
    - from: "~/.claude/skills/sync-chats/SKILL.md"
      to: "sync_chats.py cmd_scan"
      via: "Bash tool invocation"
      pattern: "sync_chats.py scan"
    - from: "~/.claude/skills/sync-chats/SKILL.md"
      to: "sync_chats.py cmd_write"
      via: "stdin JSON pipe"
      pattern: "sync_chats.py write"
---

<objective>
Add edge-case handling tests (ultra-short session skip, user message counting) and verify the complete skill works end-to-end via interactive invocation.

Purpose: LABEL-07 requires ultra-short sessions (< 2 user messages per D-05) to be skipped entirely, low-signal sessions to get a `low_signal` tag, and multi-topic sessions to get a `multi_topic` tag. The tag delegation is already handled by the prompt (Plan 02), but the ultra-short skip is Python-side logic in the skill's Bash commands that needs explicit testing. LABEL-02 requires the full scan-label-write loop to work when invoked as `/sync-chats` — this is verified via a human checkpoint.

Output: Edge case tests added, full skill verified end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-skill-md-ai-labeling/02-CONTEXT.md
@.planning/phases/02-skill-md-ai-labeling/02-RESEARCH.md
@.planning/phases/02-skill-md-ai-labeling/02-01-SUMMARY.md
@.planning/phases/02-skill-md-ai-labeling/02-02-SUMMARY.md

<interfaces>
<!-- User message counting one-liner from RESEARCH.md (verified against extract_first_user_message pattern): -->
```bash
python3 -c "
import json, sys
path = 'SESSION_PATH'
count = 0
with open(path, encoding='utf-8', errors='replace') as f:
    for line in f:
        try:
            obj = json.loads(line.strip())
            msg = obj.get('message', obj)
            if msg.get('role') == 'user':
                content = msg.get('content', '')
                if isinstance(content, str) and content.strip():
                    count += 1
                elif isinstance(content, list):
                    for b in content:
                        if isinstance(b, dict) and b.get('type') == 'text' and b.get('text','').strip():
                            count += 1
                            break
        except Exception:
            pass
print(count)
"
```

<!-- Fixture files from Plan 01: -->

- tests/fixtures/short_session.jsonl — 1 user message (should be skipped)
- tests/fixtures/multi_turn_session.jsonl — 6+ user messages (should be labeled)

<!-- sync_chats.py scan output contract: -->

```json
[
  {
    "session_id": "uuid",
    "project": "encoded-dir",
    "path": "/absolute/path.jsonl",
    "mtime": 1234567890.123,
    "size": 4096
  }
]
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add edge-case tests for ultra-short skip and user message counting</name>
  <files>tests/test_phase2_labels.py</files>
  <read_first>
    - tests/test_phase2_labels.py (current state after Plans 01 and 02)
    - tests/fixtures/short_session.jsonl (1 user message fixture)
    - tests/fixtures/multi_turn_session.jsonl (6+ user message fixture)
    - ~/.claude/skills/sync-chats/SKILL.md (the user message counting one-liner to extract and test)
  </read_first>
  <behavior>
    - Test: count_user_messages on short_session.jsonl returns 1
    - Test: count_user_messages on multi_turn_session.jsonl returns >= 6
    - Test: count_user_messages on empty file returns 0
    - Test: count_user_messages skips system-reminder messages (content containing "<system-reminder>")
    - Test: count_user_messages handles block-list content format correctly
    - Test: should_skip_session returns True when count < 2, False when count >= 2
    - Test: ultra-short skip with exactly 0 user messages -> skip
    - Test: ultra-short skip with exactly 1 user message -> skip
    - Test: ultra-short skip with exactly 2 user messages -> do NOT skip
  </behavior>
  <action>
Add to `tests/test_phase2_labels.py`:

1. A `count_user_messages(jsonl_path: str) -> int` function that replicates the user message counting logic from SKILL.md's Step 2a. This function:
   - Opens the JSONL file at `jsonl_path`
   - For each line, parses JSON, gets `msg = obj.get("message", obj)`
   - If `msg.get("role") == "user"`:
     - If content is a string: check `content.strip()` is non-empty AND does not contain `"<system-reminder>"`. If so, increment count.
     - If content is a list: check if any block has `type == "text"` and non-empty `text` that does not contain `"<system-reminder>"`. If so, increment count.
   - Returns the count.

2. A `should_skip_session(jsonl_path: str) -> bool` function that returns `count_user_messages(jsonl_path) < 2` (per D-05).

3. A `TestEdgeCases` class with tests:
   - `test_count_short_session`: assert `count_user_messages("tests/fixtures/short_session.jsonl") == 1`
   - `test_count_multi_turn`: assert `count_user_messages("tests/fixtures/multi_turn_session.jsonl") >= 6`
   - `test_count_empty_file`: create a temp empty file, assert `count_user_messages(temp_path) == 0`
   - `test_count_skips_system_reminder`: create a temp JSONL with one line where role is "user" and content contains `<system-reminder>`, assert count == 0
   - `test_count_block_list_format`: create a temp JSONL with one line where content is `[{"type": "text", "text": "Hello world"}]`, assert count == 1
   - `test_should_skip_0_messages`: empty file -> `should_skip_session` returns True
   - `test_should_skip_1_message`: short_session.jsonl -> `should_skip_session` returns True
   - `test_should_not_skip_2_messages`: create a temp JSONL with exactly 2 user messages, assert `should_skip_session` returns False
   - `test_should_not_skip_multi_turn`: multi_turn_session.jsonl -> `should_skip_session` returns False

For temp files, use `tempfile.NamedTemporaryFile(mode='w', suffix='.jsonl', delete=False)` in setUp/tearDown or within each test. Write JSONL content using `json.dumps({"message": {"role": "user", "content": "..."}}) + "\n"` format.
</action>
<verify>
<automated>cd /Users/michaelhenry/Documents/Projects/Python/claude-chat && python3 -m pytest tests/test_phase2_labels.py::TestEdgeCases -v</automated>
</verify>
<acceptance_criteria> - `tests/test_phase2_labels.py` contains `def count_user_messages(` - `tests/test_phase2_labels.py` contains `def should_skip_session(` - `tests/test_phase2_labels.py` contains `class TestEdgeCases` - `test_count_short_session` passes with count == 1 - `test_count_multi_turn` passes with count >= 6 - `test_should_skip_1_message` passes (returns True) - `test_should_not_skip_2_messages` passes (returns False) - `python3 -m pytest tests/test_phase2_labels.py -v` exits 0 with all tests passing - `python3 -m pytest tests/ -v` full suite green
</acceptance_criteria>
<done>Edge case tests verify that sessions with < 2 user messages are skipped (D-05/LABEL-07) and the counting logic handles both content formats, system reminders, and empty files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify /sync-chats skill works end-to-end</name>
  <what-built>
Complete `/sync-chats` Claude Code skill with:
- Scan for new sessions via `sync_chats.py scan`
- Ultra-short session skip (< 2 user messages)
- AI-generated labels with 4 few-shot examples (title, gist, tags, coherence_score)
- JSON extraction from Claude's ```json response
- Fallback to `make_stub_label()` on parse failure
- Safe stdin pipe to `sync_chats.py write` using json.dumps
  </what-built>
  <how-to-verify>
1. Open a NEW Claude Code session (fresh context).
2. Ensure `sync_chats.py init` has been run (check: `python3 ~/.claude-chat/sync_chats.py status` should show machine label and vault path).
3. Type `/sync-chats` and press Enter.
4. Watch Claude:
   - Run `sync_chats.py scan` and report how many sessions were found
   - For each session: read the JSONL, check user message count, extract messages, generate a label, pipe to `sync_chats.py write`
   - Skip any sessions with < 2 user messages
5. After the skill finishes, check the vault:
   - `ls ~/Library/Mobile\ Documents/iCloud~md~obsidian/Documents/*/Chats/` (adjust vault path as needed)
   - Open 2-3 of the newly created files in Obsidian or a text editor
6. Verify in each file:
   - Title is a verb-leading phrase of <= 10 words (or noun phrase for exploration)
   - Gist is 2-3 sentences in past tense
   - Tags are 3-5 kebab-case strings in YAML list format (each on its own `- ` line)
   - `coherence_score` is an integer 1-5
   - `auto_label_hash` is present (from Phase 1 pipeline)
   - `needs_review: false` (AI-generated, not stub)
7. Verify the summary line at the end shows processed/labeled/stubbed/skipped counts.

**If the skill does not load:** Check `ls ~/.claude/skills/sync-chats/SKILL.md` exists. Try `/sync-chats` again.
**If labeling fails on all sessions:** Check the fallback — files should still be written with stub labels and `needs_review: true`.
**If `disable-model-invocation: true` blocks Claude's reasoning:** Remove that line from SKILL.md frontmatter and retry. Report this as an issue.
</how-to-verify>
<resume-signal>Type "approved" if the skill works end-to-end and label quality looks good. If there are issues, describe them and we will fix before proceeding.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                              | Description                                    |
| ------------------------------------- | ---------------------------------------------- |
| JSONL content -> user message counter | Untrusted session content parsed for counting  |
| Claude label output -> Bash pipe      | AI-generated text piped to sync_chats.py write |

## STRIDE Threat Register

| Threat ID | Category               | Component                                       | Disposition | Mitigation Plan                                                                                                                        |
| --------- | ---------------------- | ----------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| T-02-01   | Tampering              | Shell injection via label JSON                  | mitigate    | `json.dumps()` serialization in SKILL.md Step 2d (carried from Plan 01). Verified during human checkpoint.                             |
| T-02-05   | Denial of Service      | Malformed JSONL line crashes counter            | mitigate    | `count_user_messages` wraps each `json.loads` in try/except, skips bad lines. Test `test_count_empty_file` verifies graceful handling. |
| T-02-06   | Information Disclosure | System reminder content counted as user message | mitigate    | Counter skips messages containing `<system-reminder>`. Test `test_count_skips_system_reminder` verifies.                               |

</threat_model>

<verification>
1. `python3 -m pytest tests/test_phase2_labels.py -v` all green
2. `python3 -m pytest tests/ -v` full suite green
3. Human verification: `/sync-chats` produces correctly labeled vault files
</verification>

<success_criteria>

- Ultra-short session skip logic tested with boundary cases (0, 1, 2 messages) (LABEL-07)
- User message counting handles both content formats and system reminders
- `/sync-chats` works end-to-end in an interactive Claude Code session (LABEL-02)
- Label quality visually verified in at least 2-3 vault files
  </success_criteria>

<output>
After completion, create `.planning/phases/02-skill-md-ai-labeling/02-03-SUMMARY.md`
</output>
